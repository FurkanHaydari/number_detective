<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/game_gradient_background">

    <TextView
        android:id="@+id/titleText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Brain Focus"
        android:textColor="@color/neonCyan"
        android:textSize="48sp"
        android:textStyle="bold"
        android:layout_marginBottom="48dp"
        android:elevation="8dp"
        android:shadowColor="@color/neonCyan"
        android:shadowDx="0"
        android:shadowDy="0"
        android:shadowRadius="15"
        app:layout_constraintBottom_toTopOf="@id/singlePlayerButton"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/singlePlayerButton"
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:text="Tek Oyunculu"
        android:textSize="18sp"
        android:padding="16dp"
        app:cornerRadius="12dp"
        app:elevation="8dp"
        android:backgroundTint="@color/primaryButton"
        app:rippleColor="@color/primaryButtonRipple"
        app:icon="@drawable/ic_play"
        app:iconGravity="textStart"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/titleText"/>

</androidx.constraintlayout.widget.ConstraintLayout>
